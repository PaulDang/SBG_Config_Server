server:
  port: 8888

spring:
  application:
    name: Config Server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/PaulDang/SBG_Config_Server
          default-label: main